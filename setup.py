# setup.py
from setuptools import setup, find_packages

setup(
    name="eigenestimation",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[],  # Dependencies
    python_requires='>=3.6',
)